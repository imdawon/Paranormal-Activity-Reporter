$(document).ready(function () {
  ('.categoryBtn').click(function() {
    const filterValue = this.val();
  });
});

  // filterValue = the array of values we want to render
  // on click of category, it re renders the page

  //add event listener for each category event delegation for whoel div
  // when clicked, anywhere on the box, if that click has value, then value form box group

